---
description: Implement a scoped task with the right role and supporting skills.
argument-hint: [task-id-or-scope]
---

Implement the requested task.

Then:

1. Use the Engineer agent by default.
2. Use the Designer agent for frontend-heavy work.
3. Pull in the narrowest relevant implementation skill.
4. Keep the change inside the task boundary.
5. End with `templates/implementation-note.md`.
