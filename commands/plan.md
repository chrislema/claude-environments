---
description: Convert project intent into a dependency-aware implementation plan.
argument-hint: [scope]
---

Produce an executable plan for the requested scope.

Then:

1. Use the Planner agent.
2. Apply `skills/planning/task-decomposition.md`.
3. Keep tasks concrete and dependency-aware.
4. Use `templates/task-plan.md`.
5. Record only true blocking decisions in `templates/decision-log.md`.
